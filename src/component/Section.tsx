import { styled } from "../constant/stitches.config";

export const Section = styled("section", {
  display: "flex",
  flexDirection: "column",
  gap: 24,
  paddingInline: 16,
  marginInline: "auto",
  maxWidth: 800,
  width: "100%",
});
